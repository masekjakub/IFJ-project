TODO
zásobník pop ze spodu - opravit
funkce na rozeznání pravidel
pravidla
bottom-up analýza výrazů
struktura stromu
návrh gramatiky
rozsireni globalni promenne?

Kubík - return do nextRule

GRAMATIKA

<prog> => BEGIN DECLARE_ST <statement_list> <end>
<end> => EPILOG EOF
<end> => EOF
<id> => ID
<id> => FUNID

<stat_list> => <stat> <stat_list>
<stat> => <assign>
<stat> => IF (<expr>) {<stat>} else {<stat>}
<stat> => WHILE (<expr>) {<stat>}
<stat> => FUNCTION FUNID(<params>) : TYPE {<stat>}
<stat> => FUNCTION FUNID(<params>) {<stat>} ??????????????

<assign> => ID = <expr> ;
<assign> => ID <expr> ;
<assign> => <expr> ;

<params> => TYPE ID
<params> => ? TYPE ID
<params> => TYPE ID, <params>
<params> => ? TYPE ID, <params>

<args> => <id>
<args> => <id>, <args>

<return> => RETURN <expr> ;
RETURN; ???

------------------------------------
    E => ID
    E => E + E
    E => E - E
    E => E * E
    E => E / E
    E => (E)
        

<value> => INT 
<value> => FLOAT
<value> => STRING
<value> => <id>

    FUNCCALL => funid(ARGS);

    TYPE => ?TYPE
    TYPE => int_type
    TYPE => float_type
    TYPE => string_type