TODO
zásobník pop ze spodu
funkce na rozeznání pravidel
pravidla
bottom-up analýza výrazů
struktura stromu
návrh gramatiky

GRAMATIKA
    E => id
    E => E + E
    E => E - E
    E => E * E
    E => E / E
    E => (E)


    S => id = E;
    S => IF
    S => WHILE
    S => FUNCDEF
    S => id = FUNCCALL
    S => S S

    IF => if (E) {S} else {S}
    WHILE => while (E) {S}
    FUNCCALL => function(ARGS);
    FUNCDEF => function(PARAMS) : TYPE {S}
    TYPE => ?TYPE
    TYPE => int
    TYPE => float
    TYPE => string
    ARGS => id
    ARGS => id,ARGS
    PARAMS => TYPE id
    PARAMS => TYPE id, PARAMS
    ID 
    $ID 
    EPILOG => ?> eof
    EPILOG => eof



    $afdfg = E;

